<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Verificar MFA</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background: linear-gradient(to right, #6a11cb, #2575fc);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .verify-card {
      background: white;
      border-radius: 1.5rem;
      padding: 2rem;
      box-shadow: 0 0 30px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 450px;
      text-align: center;
    }
    .verify-card h4 {
      font-weight: 600;
      margin-bottom: 1.5rem;
    }
    .btn-primary {
      background-color: #2575fc;
      border: none;
    }
    .btn-primary:hover {
      background-color: #1a5fdc;
    }
  </style>
</head>
<body>

  <div class="verify-card">
    <% if (qrCode) { %>
      <h4>Escanea este código QR</h4>
      <p>Ábrelo con Google Authenticator o Microsoft Authenticator</p>
      <img src="<%= qrCode %>" alt="QR Code" class="img-fluid mb-3" />
    <% } else { %>
      <h4>Verificación en dos pasos</h4>
      <p>Ingresa el código generado en tu app de autenticación</p>
      <form action="/verify" method="post">
        <input type="hidden" name="email" value="<%= email %>">
        <div class="mb-3">
          <input type="text" name="token" class="form-control text-center" placeholder="123456" required />
        </div>
        <button type="submit" class="btn btn-primary w-100 mb-2">Verificar código</button>
      </form>
    <% } %>

    <a href="/login" class="btn btn-link w-100">← Volver al login</a>
  </div>

</body>
</html>
